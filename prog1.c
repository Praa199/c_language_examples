#include <stdio.h> //should be included at the beginning of just about every program

int main(void) //main() is a special name that indicates precisely where the program is to begin execution
               //and that it returns an integer value
{
    printf("Programming is fun.\n");
    return 0; // return to the system a status value of 0
}