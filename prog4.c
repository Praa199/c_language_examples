#include <stdio.h>
int main(void)
{
    int sum;
    sum = 50 + 25;
    printf("The sum of 50 and 25 is %i\n", sum); // percentage specifies what type of value is to be displayed at that point
    return 0;
}